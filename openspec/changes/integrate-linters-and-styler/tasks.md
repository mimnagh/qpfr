## 1. Implementation
- [ ] Add `.lintr` configuration with project rules.
- [ ] Add `scripts/style.R` that runs `styler::style_dir('R')` and a `make style` target.
- [ ] Add CI check step for style in check-only mode.
- [ ] Optionally run `styler` and open a PR with formatting fixes.

## 2. Review
- [ ] Ensure lintr rules are agreeable to maintainers.

## 1. Implementation
- [ ] Add config validation function (e.g., `validate_optimizer_config(config)`).
- [ ] Call validation early in `DataProvider::load()` with clear errors.
- [ ] Add unit tests and a malformed-example YAML fixture under `tests/fixtures/`.

## 2. Review
- [ ] PR review and update docs with expected config schema.

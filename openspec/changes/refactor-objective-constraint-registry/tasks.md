## 1. Implementation
- [ ] Add `R/ObjectiveRegistry.R` implementing registration and lookup of objectives and constraints.
- [ ] Update `PortfolioOptimizer` to accept a registry instance (DI) or to instantiate the registry.
- [ ] Add unit tests for registry behavior and integration tests for optimizer using a mock registry.

## 2. Design
- [ ] Create `design.md` describing public API, plugin patterns, and migration steps.
